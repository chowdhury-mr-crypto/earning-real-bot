# Earning Real Telegram Bot

Starter Telegram task/earning bot for @earningrealbot.

## Run
1. Install Python 3.11+.
2. Run: `pip install -r requirements.txt`
3. Set the `BOT_TOKEN` environment variable.
4. Run: `python bot.py`

Never put the BotFather token in GitHub.
